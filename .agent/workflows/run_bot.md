---
description: Build the entire solution and run the BotHub application
---

To build the entire solution (including Bots) and run the application, execute the following command in the terminal:

```powershell
./run_bot.ps1
```
